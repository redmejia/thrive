import { SafeAreaView } from "react-native"
import Orders from "../Screens/Menu";

const Main = () => {
    return (
        <SafeAreaView style={{ flex: 1}}>
            <Orders />
        </SafeAreaView>
    )
}

export default Main;